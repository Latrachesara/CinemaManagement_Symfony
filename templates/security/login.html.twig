{% extends 'base.html.twig' %}
{% block body %}
    <style>
        form {
            margin-right: 1100px;
            margin-left: 30px;
        }
        h1 {
            margin-left: 30px;
        }
        a {
            color: white;
            text-decoration: none;
        }
    </style>
    <br>
    <h1>Connexion</h1>
    <br>
    <form action="{{ path('security_login') }}" method="post">
        <div class="form-group">
            <label>Adresse Email : </label>
            <input placeholder="Adresse Email" name="_username"  type="text" class="form-control" required>
        </div>

        <div class="form-group">
            <label>Mot de passe : </label>
            <input placeholder="Mot de passe" name="_password"  type="password" class="form-control" required>
        </div>
        <br><br>


        <button type="submit" class="btn btn-secondary"> <a href="{{ path('accueil')}}">Connexion</a></button>

        <br><br>


            <button type="submit" class="btn btn-secondary"> <a href="{{ path('security_registration')}}"> Inscription</a> </button>



    </form>


{% endblock %}
