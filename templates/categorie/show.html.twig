{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <style>
        a {
            color: white;
        }
    </style>
    <br>  <br>
    <h1>Categorie</h1>
<br>
    <table class="table">
        <tbody>
            <tr>
                <th>Numéro</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie.nom }}</td>
            </tr>
        </tbody>
    </table>

<button type="button" class="btn btn-secondary"><a href="{{ path('categorie_index') }}">back to list</a></button>

    <button type="button" class="btn btn-secondary"><a href="{{ path('categorie_edit', {'id': categorie.id}) }}">edit</a></button>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
